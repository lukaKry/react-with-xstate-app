import React from 'react'

export default function SwLoading() {
  const style= {
    width: '100vh',
    height: '100vh',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  }
  
  return (
    <div style={style}>loading...</div>
  )
}
